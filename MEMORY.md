# MEMORY.md — baza_bot

Живой контекст проекта. Внешняя память Claude между сессиями. Пополняется **в конце каждой задачи**.

**Последнее обновление:** 2026-07-16

---

## Архитектурные решения

### Двухуровневый ответ: alias-роутинг + project-scoped контекст

- **Решение:** вопрос → keyword fast-path (canned) → регистронезависимый alias-роутер проекта → весь корпус проекта в контекст LLM. Retrieval внутри проекта не нужен — корпус одного ЖК мал.
- **Дата:** 2026-07-16
- **Контекст:** база знаний ~60k токенов, 6 ЖК; классический RAG путал проекты («Алиса» — и ЖК, и колонка).
- **Альтернативы (все отвергнуты по живым замерам):** full-context 69k (галлюцинирует — приписал ДМД2 потолки из ЖК ДНК), наивный RAG top-6 (3 провала из 4), RAG+реранк (сломал ДМД2), семантический роутер проекта (зазор score 0.03).
- **Почему так:** project-scoped дал 4/4 верных ответа; роутинг детерминирован справочником заказчика; худший случай 0.259¢ и 5.28s при лимитах ТЗ 5¢/30s.

### Реестр проектов — единственный источник истины

- **Решение:** `server/registry/projects.ts` задаёт для 11 проектов алиасы, регион (ЕКБ/МСК/Бали), класс (застройщик/агентство), комиссию. Проект НЕ угадывается из пути файла.
- **Дата:** 2026-07-16
- **Контекст:** прототип приписал `презентация_днк.pdf` Бестселлеру по строке пути — ЖК ДНК исчез как проект. Цена ошибки: ДМД1 2.5% против ДМД2 2.7%.
- **Почему так:** комиссия и регион определяют ответ (менеджер, ссылка на рассрочку, «Проекты ПИФ»).

### Источник истины по условиям — партнёрский портал, не таблица тест-кейсов

- **Решение:** комиссии, рассрочка, ипотека — из `realtors.baza.bz` (дерево 15 страниц). Таблица тест-кейсов — фолбэк.
- **Дата:** 2026-07-16
- **Контекст:** живой свод `/ekb/svod` расходится с таблицей по Алисе (2,6%+1% на выбор против 2.7%). Решение владельца: истина за порталом.
- **Почему так:** портал актуальнее и опубликован; ТЗ запрещает выдумывать условия. Комиссия — структура `{base, bonus, bonusRule}`, не число (агенту дают 1% на выбор: себе или скидкой клиенту).

---

## Сделано (recent)

- **2026-07-16** — **Часть 1 ТЗ реализована по коду** (Phase 1–3 плана в `main`, 13 коммитов):
  - Phase 1: реестр (13 проектов, 17 источников) + ingest (642 чанка по 6 ЖК, дедуп, идемпотентность).
  - Phase 2: ядро ответа (интент-роутер + project-scoped RAG + canned) — 30 unit-тестов, 11/12 acceptance на живой БД. Бот `@baza_parthners_bot` (grammY long polling) работает локально.
  - Phase 3: голосовые (Soniox STT через REST, байты не URL) — проверено на живом ogg (5.8s).
  - README (инструкция запуска + обоснование архитектуры + out-of-scope).
- **2026-07-16** — План v13 APPROVED после 13 кругов `/review-plan`. Claude Code инициализирован.

---

## В работе сейчас

- **Часть 1 готова по коду, бот живой локально.** Осталось внешнее (требует решения пользователя):
  - **GitHub-репозиторий** — создать и запушить (ТЗ требует; заблокировано auto-mode до решения пользователя об аккаунте/видимости).
  - **Постоянный хостинг** — сейчас бот на локальной машине; для сдачи деплой на домашний сервер (long polling, Docker prod stack готов).
- **Токен бота** `@baza_parthners_bot` в `.env` (id 8718895181).
- Далее по ТЗ: **Часть 2** (админка shadcn), **Часть 3** (страница Figma) — когда пользователь скажет.

---

## TODO / в планах

- Phase 1: реестр 11 проектов + ingest 17 источников + обход дерева портала → pgvector.
- Phase 2: ядро ответа + живой бот в Telegram (деплой через Cloudflare Tunnel).
- Phase 3: голосовые (Soniox).
- Части 2–3 (админка, Figma) — вне текущего плана.

---

## Известные проблемы / ограничения

- **Google/Yandex троттлят 503/429** при частых скачиваниях — в `http.ts` retry 5× с backoff + паузы 800мс между binary-источниками. При первом холодном прогоне может потребоваться 2 запуска (идемпотентность добьёт).
- **Идемпотентность документа — по наличию чанков**, не только по hash: OK-документ с 0 чанков (эмбеддинг упал на прошлом прогоне) считается битым и пересоздаётся. Иначе битый документ пропускался бы навсегда.
- **Сайты Tilda переэмбеддятся каждый прогон** (контент чуть плавает → новый hash). Не дубли (старый документ источника удаляется), но лишний расход. Приемлемо на 2 сайтах.
- **TypeScript 5.9.3, не 7.0**: typescript-eslint 8.x требует TS `<6.1.0`. TS 7 (Go-порт) тулчейн ещё не догнал.
- **Портал `realtors.baza.bz` троттлит 403** при быстрых запросах подряд — ingest портала идёт headed + паузы ~1.2с.
- **best.baza.bz блокирует headless** (заглушка BotFAQtor) — парсинг только headed chromium + xvfb.
- **Tailscale на сервере не поднят** — удалённо чинить бота нельзя, только из дома. Поднять до защиты.
- **Кэш bothub недетерминирован** — на него не закладываться.
- **Данные сессии планирования** (индекс, корпус, дерево портала) лежали в `/tmp/.../scratchpad/` — временно. Ingest пересоберёт из источников.

---

## Что НЕ работает как ожидалось (анти-решения)

- **Full-context** — галлюцинирует, путает проекты. Отвергнут.
- **Наивный RAG top-k по косинусу** — соседние разделы вытесняют нужный чанк. Отвергнут.
- **RAG + LLM-реранк** — нужный чанк не доходит до top-30. Отвергнут.
- **Семантический роутер проекта (эмбеддинги)** — зазор 0.03 между верным и ложным. Отвергнут → детерминированные алиасы.
- **Атрибуция проекта по строке пути** — теряет ЖК. Отвергнута → явный реестр.

---

## External integrations — состояние

- **bothub** — ключ живой (в PDF ТЗ разбит переносами, склеивать аккуратно). gpt-4.1-nano + bge-m3 (1024). snake_case работает. Prompt caching недетерминирован.
- **Soniox** — ключ живой. `stt-async-v5`, русский. OGG/Opus напрямую (без ffmpeg). Опция `api_key` (не `apiKey`); ошибки асинхронные, проверять `status`.
- **Telegram** — токен получить у @BotFather (шаг 0 Phase 2). Референс: @bazabz_broker_bot.
- **Yandex Disk** — имена файлов в **NFD** (буква «й» ломает матч по имени) → нормализовать NFC.
- **Партнёрский портал** — троттлит 403, деревья ЕКБ/МСК несимметричны (`/msk/svod` = 404).

---

## Последняя сессия — summary

- **Дата:** 2026-07-16
- **Что делали:** 13 кругов планирования+ревью, план APPROVED; инициализация проекта (init-full).
- **Что не успели:** код Phase 1 (следующий шаг).
- **Блокеры:** нет.
- **Next:** `git init` → `/ship-it` Phase 1 (реестр + ingest).

---

## Правила ведения MEMORY.md

1. Обновляй в конце задачи, не в начале. В начале — читай.
2. Архитектурные решения — только важные.
3. Recent ограничивай 20-30 записями.
4. Known issues фиксируй сразу.
5. Не дублируй git log.
