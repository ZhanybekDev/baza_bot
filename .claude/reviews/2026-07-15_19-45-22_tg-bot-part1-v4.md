# Plan Review — tg-bot-part1 (v4)

**Timestamp:** 2026-07-15 19:45:22
**Verdict:** `APPROVE_WITH_CHANGES`
**Complexity:** HIGH
**Plan source:** `/Users/mak/Desktop/amz/baza_bot/.claude/plans/current-plan.md`
**Files touched (по плану):** ~28 (greenfield)
**Probed assumptions:** 6
**Failed assumptions:** 1
**Applied fixes:** 5

**HARD RULES CHECK: skipped — в проекте нет `CLAUDE.md`.** Правила взяты из глобальной user-memory (worktree, pre-commit, без Co-Authored-By, NO FALLBACKS) — см. M1.

---

## Контекст ревью

Это ревью **патчей предыдущего ревью**. v3 был помечен `APPROVE_WITH_CHANGES`, и 5 правок применил я сам — их никто не проверял. Основной улов пришёлся ровно на них: один патч оставил в плане прямое самопротиворечие.

---

## Assumption Register

| # | Assumption | Проверка | Статус |
|---|---|---|---|
| 1 | Проект — git-репозиторий | `git status` → `fatal: not a git repository` | 🔴 **FAILED** |
| 2 | План покрывает требования ТЗ к сдаче | `grep -niE "git\|commit\|коммит\|репозитор\|github"` → **0 совпадений** | 🔴 **FAILED** |
| 3 | Патчи v3 согласованы с телом плана | Строка 224 vs 76/152/243 | 🔴 **противоречие** |
| 4 | Замеры задержки/цены | Перепроверены в v3 | ✅ |
| 5 | Архитектура подтверждена | 4/4 | ✅ |
| 6 | Оценки фаз сходятся | 2.5–3 + 2.5–3 + 1–1.5 = 6–7.5 ✅ | ✅ |

---

## Missing Steps (applied)

### M1. Git-процесс отсутствует в плане целиком — а ТЗ его оценивает
- **Dimension:** D11 (Output Verification) / Missing Step
- **Where:** нигде. `grep` по плану на `git|commit|коммит|репозитор|github` → **0 совпадений**
- **Problem:** ТЗ в разделе «Результат и формат отправки» требует дословно:
  > Плюс репозиторий на GitHub (можно приватный, дай доступ) с:
  > — инструкцией запуска
  > — обоснованием выбора архитектуры поиска по знаниям
  > — **нормальной историей коммитов. Один коммит «init» со всем кодом — плохой знак**
  > — если работал через Claude Code или аналог — оставь CLAUDE.md и файлы контекста в репо

  План покрывает README (обоснование) и CLAUDE.md, но **git не упоминает ни разу**, а `/Users/mak/Desktop/amz/baza_bot` даже не инициализирован как репозиторий. «История коммитов» — это не побочный артефакт, а **явный критерий оценки**: её нельзя дописать в конце, она формируется по ходу трёх фаз. План, который не планирует коммиты, гарантированно приведёт к тому самому «одному коммиту init».
- **Blast radius:** тот же класс, что и «ловушка ДМД2» в v1 — требование ТЗ, вылетевшее из плана целиком.
- **Fix applied:** добавлена секция «Git-процесс» + git-шаги в Implementation outline каждой фазы + acceptance criteria. Учтены глобальные правила пользователя (worktree для >2 файлов, pre-commit `lint`+`build`+тесты, коммиты без `Co-Authored-By`).

---

## Critical Findings

### C1. Патч v3 оставил прямое самопротиворечие про кэш
- **Dimension:** D1 (Domain Model Audit) — консистентность собственных утверждений
- **Where:** строка 224 (Assumptions) против строк 76, 152, 243
- **Problem:**
  - стр. 76: «Кэш bothub **недетерминирован — на него нельзя опираться**»
  - стр. 152: «Prompt caching **не проектируем** — он недетерминирован»
  - стр. 243: «Кэш недетерминирован → не закладываться»
  - стр. 224: «| Кэш bothub работает | 4-й прогон: `cached=69 248`, цена ×4 вниз | ✅ **прогревается не сразу** |»

  Строка 224 — цитата из **опровергнутого** замера (bench3, старый корпус). Реальные данные v3: Алиса `cached = 0 / 20 224 / 0`, Бестселлер `0 / 0 / 25 472` — попадание не гарантировано даже на идентичном префиксе подряд. Я исправил тело плана, но забыл строку в таблице assumptions.
- **Fix applied:** строка 224 приведена в соответствие с замером.

### C2. «6 ЖК» против «7 проектов» — критерий приёмки проверяет не то множество
- **Dimension:** D2 (Cross-Component Pipeline)
- **Where:** стр. 5 («7 проектов») против стр. 111 («у каждого из **6 ЖК** чанков > 0»)
- **Problem:** В индексе 7 сущностей: 6 ЖК + `ОБЩЕЕ` (14 чанков, регламент по работе с партнёрами). Критерий «у каждого из 6 ЖК чанков > 0» **не покрывает `ОБЩЕЕ`** — а это единственный источник по теме «вознаграждения агентов», ключевой для аудитории бота. Регламент мог бы выпасть, и инвариант остался бы зелёным.
- **Fix applied:** критерий переписан на 7 проектов с явным упоминанием `ОБЩЕЕ`.

### C3. Критерий задержки непроверяем и конфликтует с замером
- **Dimension:** D11 (Output Verification)
- **Where:** стр. 166 — «Простой вопрос **< 5s и < 1 цента**; худший случай (Бестселлер, без кэша) **< 6s и < 0.3¢**»
- **Problem:** «Простой вопрос» нигде не определён. Если это canned — то ~0s, и порог 5s бессмыслен. Если project-scoped — то замер дал **5.28s** на Бестселлере, и критерий «< 5s» **упал бы**. Два порога на одной строке противоречат друг другу.
- **Fix applied:** критерии разведены по трём реальным веткам архитектуры с порогами из замеров.

### C4. Токены ДМД2: 4k против 5.6k в одном плане
- **Dimension:** D1
- **Where:** стр. 223 («ДМД2 ~4k ток / 0.03¢») против стр. 68 («ДМД2 / ДНК / Каменные Палатки | ~5.6k»)
- **Problem:** Обе цифры «правильные», но разные по смыслу: 3 971 — корпус проекта, 5 577 — `prompt_tokens` целиком (корпус + system + вопрос). В плане они поданы как одна величина. Плюс стр. 68 схлопывает ДМД2/ДНК/КП в «~5.6k», хотя реально 3.4–5.6k, а стр. 69 даёт ДМД1 те же «~5.6k» при 46 чанках против 37.
- **Fix applied:** цифры разведены на «корпус проекта» и «prompt_tokens», диапазоны уточнены.

---

## Dimensions без findings

D3 (Cost), D4 (Self-Critique), D5 (Waste), D6 (Failure Modes), D7 (Code Health), D8 (Abstraction), D9/D10/D12 — чисто. Архитектура подтверждена замерами, три конкурента отвергнуты по данным, бюджет с запасом 19×.

---

## Applied Fixes (summary)

- **[M1]** Git-процесс: секция + шаги в каждой фазе + acceptance; worktree, pre-commit, без `Co-Authored-By`, GitHub-репо с доступом
- **[C1]** Строка assumptions про кэш приведена в соответствие с замером (недетерминирован)
- **[C2]** Инвариант атрибуции: 7 проектов вместо 6 ЖК, с явным `ОБЩЕЕ` (регламент)
- **[C3]** Критерии задержки разведены по веткам архитектуры, пороги из замеров
- **[C4]** Разведены «корпус проекта» и «prompt_tokens», уточнены диапазоны

---

## Unresolved

Нет.

---

## Verdict Rationale

BLOCKING нет. Архитектурное ядро проверено замерами и осталось нетронутым.

Главная находка — **git-процесс, отсутствующий в плане целиком** при том, что ТЗ оценивает историю коммитов явно («один коммит init — плохой знак»), а проект даже не инициализирован. Это требование нельзя добрать в конце: история пишется по ходу фаз. Тот же класс ошибки, что «ловушка ДМД2» в v1 — пункт ТЗ, выпавший из поля зрения.

Остальные четыре — про внутреннюю согласованность, и три из них внесены **моими же патчами v3**: строка про кэш осталась опровергнутой, критерий «6 ЖК» не покрывает `ОБЩЕЕ`, пороги задержки конфликтуют. Все патчатся точечно → `APPROVE_WITH_CHANGES`.

---

## Next Action

`/init-full` (в проекте нет `CLAUDE.md` — Hard Rules Check пропущен в четырёх ревью подряд), затем `git init` и `/ship-it` по Phase 1.
