# Plan Review — tg-bot-part1 (v6)

**Timestamp:** 2026-07-16 00:05:00
**Verdict:** `APPROVE_WITH_CHANGES`
**Complexity:** HIGH
**Plan source:** `/Users/mak/Desktop/amz/baza_bot/.claude/plans/current-plan.md`
**Probed assumptions:** 5
**Failed assumptions:** 1
**Applied fixes:** 4

**HARD RULES CHECK: skipped — в проекте нет `CLAUDE.md`** (шестое ревью подряд).

---

## Контекст

Третье подряд ревью собственных патчей. v5 я закрыл предсказанием, что следующий круг не окупится. Он окупился — но в последний раз, и по той же причине, что и два предыдущих: **требование ТЗ, процитированное в плане, но не доведённое до места, где оно проверяется**.

---

## Assumption Register

| # | Assumption | Проверка | Статус |
|---|---|---|---|
| 1 | План покрывает все требования ТЗ к репо | `grep -niE "инструкц\|запуска"` → 1 совпадение, и то **внутри цитаты ТЗ** | 🔴 **FAILED** |
| 2 | Патч v5 не поломал структуру | 3 outline + 1 упоминание = 4; дубликатов нет; Phase 3 outline на месте | ✅ |
| 3 | Test strategy у всех фаз | `grep -c` → **2** из 3 | 🔴 у Phase 3 не было |
| 4 | Оценки сходятся | 2.5–3 + 2.5–3 + 1–1.5 = 6–7.5 ✅ | ✅ |
| 5 | Архитектурное ядро | Не менялось с v3, подтверждено замерами | ✅ |

---

## Missing Steps (applied)

### M1. «Инструкция запуска» — требование ТЗ №1 к репозиторию — нигде не запланирована
- **Dimension:** D11 (Output Verification)
- **Where:** ни одного `What changes`, ни одного acceptance-критерия
- **Problem:** ТЗ перечисляет содержимое репозитория в таком порядке:
  > Плюс репозиторий на GitHub (можно приватный, дай доступ) с:
  > — **инструкцией запуска**
  > — обоснованием выбора архитектуры поиска по знаниям
  > — нормальной историей коммитов…

  «Инструкция запуска» стоит **первой**. В плане она встречается **ровно один раз — внутри цитаты самого ТЗ** (стр. 89). README при этом расписан подробно: обоснование архитектуры, таблица 4 замеров, секция STT, задержки по веткам, решение по Бали. Как всё это запустить — не запланировано нигде.
- **Blast radius:** тот же класс, что пропуск git в v3 и неполный git-патч в v4. Требование видно в тексте ТЗ, процитировано в плане — и всё равно не доехало до acceptance. Инструкция, восстановленная по памяти в конце, не будет воспроизводимой: половина шагов (порт 5433 вместо 5432, `CREATE EXTENSION vector` в миграции, xvfb для headed chromium, `ingress` в cloudflared) — неочевидные решения, принятые по ходу фаз.
- **Fix applied:** в секцию «Git-процесс» добавлены три обязательных блока README с явной инструкцией запуска; в Phase 1 — критерий «инструкция запуска ingest проверена на чистой машине»; в Phase 2 — «инструкция дополнена до полной (локально + деплой), по ней поднимается с нуля».

### M2. У Phase 3 не было Test strategy
- **Dimension:** D11
- **Where:** Phase 3 — после Implementation outline сразу шла секция Assumptions
- **Problem:** Phase 1 и Phase 2 имеют отдельную Test strategy, Phase 3 — нет. Содержательно тесты у неё в acceptance есть (мок Soniox, живой ogg), но структурная асимметрия: ship-it читает Test strategy как отдельный блок.
- **Fix applied:** добавлена Test strategy для Phase 3 (unit на моках + ветка `status=error`, integration на живом ogg, manual — сверка голосового ответа с текстовым аналогом).

---

## Dimensions без findings

D1–D10, D12 — чисто. Структура после патчей v5 проверена: дубликатов нет, Phase 3 outline на месте, оценки сходятся, архитектурное ядро не менялось с v3 и подтверждено замерами.

---

## Applied Fixes (summary)

- **[M1]** README: три обязательных блока по ТЗ, с инструкцией запуска первым
- **[M1a]** Phase 1: критерий «инструкция запуска ingest воспроизводится на чистой машине»
- **[M1b]** Phase 2: критерий «инструкция дополнена до полной — локально + деплой»
- **[M2]** Phase 3: добавлена Test strategy

---

## Unresolved

Нет.

---

## Verdict Rationale

BLOCKING нет. Архитектурное ядро не менялось с v3, фактура перепроверена и держится.

Одна находка — существенная и того же класса, что два предыдущих круга: **«инструкция запуска», первое требование ТЗ к репозиторию, процитирована в плане и не доведена ни до одного acceptance**. Это третий подряд случай, когда пункт ТЗ виден глазами, но не превращён в проверяемый критерий. Вторая находка — структурная асимметрия Phase 3. Обе патчатся точечно → `APPROVE_WITH_CHANGES`.

**Рекомендация по процессу: это последний окупившийся круг.** Все три требования ТЗ к репо (инструкция, обоснование, история коммитов) теперь имеют владельца-критерий. Дальше ревью будет ловить формулировки, а не дыры. План исполним.

---

## Next Action

`/init-full` → `git init` → `/ship-it` по Phase 1. Дальнейшие прогоны `/review-plan` не рекомендуются: findings исчерпаны, ядро стабильно три круга.
